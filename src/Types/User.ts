export type User = {
  id: number;
  firstName: string;
  secondName: string;
  birthdate: string;
  streetAdress: string;
  city: string;
  state: string;
  zipCode: string;
  mail: string;
  phoneNumber: string;
  imageSource: string;
};

export const userType: User[] = [];
